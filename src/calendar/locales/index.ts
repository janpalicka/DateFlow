export { cs } from "./cs";
export { de } from "./de";
export { en } from "./en";
export { fr } from "./fr";

import { en } from "./en";

/** Default English (US) locale; `firstDayOfWeek` is Sunday. */
export const DEFAULT_LOCALE = en;
